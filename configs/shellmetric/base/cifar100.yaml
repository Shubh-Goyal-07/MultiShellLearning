include: cifar_resnet18.yaml

cache_dir: artifacts/cache/cifar100

data:
  dataset: cifar100
  root: data
  split_manifest: artifacts/splits/cifar100.json
  split_seed: 12345
  validation_fraction: 0.10
  sealed_test: true
